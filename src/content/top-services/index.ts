// Compatibility entry point: the content now lives in src/content/services/ (one file
// per category). Importers of "@/content/top-services" see the exact same map.
export { TOP_SERVICE_CONTENT } from "@/content/services";
export type { TopServiceContent } from "./types";
